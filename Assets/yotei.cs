using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class yotei : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
//マウスの移動距離をunityちゃんのrotationに代入すればマウスでaimできる？
//右クリした時カメラのconstraintのunity-chanの座標を変える→照準からrayを飛ばした先にemptyをワープさせ，そこにparent constraint